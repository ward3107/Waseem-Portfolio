sonar.projectKey=ward3107_Waseem-Portfolio
sonar.organization=ward3107

# The per-city local SEO landing pages under public/web-design-*/ are generated
# from a single template (.dev-scripts/generate-landing-pages.mjs). They share
# structure and inline CSS by design, so they are excluded from analysis and
# duplication detection rather than treated as hand-maintained source.
sonar.exclusions=public/web-design-*/**
sonar.cpd.exclusions=public/web-design-*/**
